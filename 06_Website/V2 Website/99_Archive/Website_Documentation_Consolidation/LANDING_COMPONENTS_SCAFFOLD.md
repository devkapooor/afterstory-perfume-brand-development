# Landing Components

Reserved for a future V2 landing-page rebuild. Add focused components here only when that page is approved for implementation, for example `Header`, `Introduction`, `Campaign`, `LaunchScene`, and `Footer`.

The active V2 shop uses `components/v2/` and does not import this folder.
