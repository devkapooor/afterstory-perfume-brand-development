# Shop Components

Reserved for reusable V2 shop features that are broader than the current launch storefront, such as filters, category navigation, recommendations, and checkout sections.

The current launch collection and product components remain in `components/v2/` until a feature needs to be shared beyond that flow.
